# Seerlens dashboard

The React + TypeScript frontend for [Seerlens](../README.md). The collector builds and serves it; you rarely run it standalone.

```bash
npm install
npm run build   # outputs into ../src/Seerlens.Collector/ui
npm run dev     # local dev server, proxies the API to http://localhost:5005
```
